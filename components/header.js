import styles from 'styles/header.module.css'

export default function header () {
  return (
    <header className={styles.header}>
      <p className={styles.title}>POKEMON RANDOM API
      </p>
    </header>
  )
}
